#pragma once
#include <Arduino.h>

const String msg_invalid_format = F("# Msg format invalid");
const String msg_invalid_index = F("# Invalid focuser index");
const String msg_zeroing_done = F("# Focuser zeroed");