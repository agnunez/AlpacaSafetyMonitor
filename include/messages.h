#pragma once
#include <Arduino.h>

const String msg_invalid_format = F("# Msg format invalid");
const String msg_invalid_index = F("# Invalid focuser index");
const String msg_invalid_arg = F("# Invalid argument");
const String msg_invalid_reg = F("# Invalid register");
const String msg_invalid_cmd = F("# Invalid command");
const String msg_zeroing_done = F("# Focuser zeroed");
